# 席替えアプリ

教室の座席をランダムに割り当てるための Web アプリケーション。

## 機能

- **生徒情報の読み込み**: CSV ファイルまたはテキスト貼り付けで生徒データを登録
- **座席レイアウト設定**: 行数・列数の変更、使用不可席（障害物など）の指定
- **固定座席設定**: 特定の生徒を特定の座席に固定
- **ルーレット**: アニメーションで座席をランダム点灯し、1 人ずつ座席を決定（全員一括割り当ても可能）
- **座席調整**: ドラッグ&ドロップで座席を入れ替え
- **出力**: PDF 出力、または表計算ソフト向けのクリップボードコピー
- **データ保存**: ローカルストレージへの保存・読み込み（ページリロード後も復元可能）

## 画面フロー

```
生徒情報入力 → 座席レイアウト設定 → 固定座席設定 → ルーレット → 座席確認・調整 → 出力
```

## 技術スタック

| カテゴリ | 採用技術 |
|----------|----------|
| フレームワーク | React 19 + TypeScript 5.8 |
| ビルドツール | Vite 6 |
| UI ライブラリ | MUI (Material UI) v7 |
| ドラッグ&ドロップ | @hello-pangea/dnd |
| CSV 解析 | PapaParse |
| PDF 出力 | jsPDF + html2canvas |

## セットアップ

```bash
# 依存パッケージのインストール
npm install

# 開発サーバーの起動
npm run dev
```

開発サーバー起動後、ブラウザで http://localhost:5173 を開く。

## コマンド

```bash
npm run dev      # 開発サーバー起動
npm run build    # 本番ビルド
npm run preview  # ビルド結果のプレビュー
npm run lint     # ESLint 実行
```

## Docker での起動

```bash
docker-compose up
```

## 設計ドキュメント

- [アーキテクチャ](docs/architecture.md) — 技術スタック・状態管理・ファイル構成
- [画面フロー](docs/app-flow.md) — フェーズ遷移・各画面の責務
- [データモデル](docs/data-model.md) — 型定義の意図と制約
- [コンポーネント仕様](docs/components.md) — 各コンポーネントの責務と Props
- [実装計画](docs/roadmap.md) — 未実装機能・既知の課題

## CSV フォーマット

以下の列を含む CSV を使用する（区切り文字はカンマ・タブ・セミコロン・パイプに対応）。

| 列名 | 説明 | 必須 |
|------|------|------|
| 番号 | 出席番号 | 推奨 |
| 氏名 | 生徒の氏名 | 推奨 |
| ふりがな | 氏名のふりがな | 任意 |
| 情報1 | 追加情報（例: 性別） | 任意 |
| 情報2 | 追加情報（例: 出身校） | 任意 |
| 情報3 | 追加情報（例: 特記事項） | 任意 |
